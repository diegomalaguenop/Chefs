@model IEnumerable<Chefs.Models.Plato>

<h1>Lista de Platos</h1>

<table class="table">
    <thead>
        <tr>
            <th>Nombre del Plato</th>
            <th>Chef</th>
            <th>Sabor</th>
            <th>Calorías</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var plato in Model)
        {
            <tr>
                <td>@plato.Nombre</td>
                <td>@plato.Chef.Nombre</td>
                <td>@plato.Sabor</td>
                <td>@plato.Calorias</td>
            </tr>
        }
    </tbody>
</table>
